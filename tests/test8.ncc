routine abs(a: real): real is
    if a >= 0.0 then
    else
        a := a * -1
    end
    return a
end

routine main() is
    assert abs(-4.4), 4.4 // OK
    print(abs(-4.4))
    assert abs(0.0), 0.0 // OK
    print(abs(0.0))
    assert abs(3.14), 3.14 // OK
    print(abs(3.14))
end