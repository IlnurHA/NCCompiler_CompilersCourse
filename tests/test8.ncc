routine abs(a: real): real is
    if a >= 0 then
    else
        a := a * -1
    end
    return a
end

routine main() is
    assert abs(-4.4), 4.4 // OK
    assert abs(0.0), 0.0 // OK
    assert abs(3.14), 3.14 // OK
end