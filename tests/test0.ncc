type node is
    record
        var a is [1, 2, 3, 4]
    end

routine foo(): integer is
    var x is 5
    
    if x = 5 then
        return 2
    else 
    end
    return 1
end

routine main() is 
   var a:
   record 
    var hello: integer
    var ends: boolean
   end
   a.hello := integer(true)
   
   var b: node
   var new_arr is b.a.sorted
   var arrayOfArrays: array[4]array[4]integer
   arrayOfArrays[0] := b.a.reversed
end